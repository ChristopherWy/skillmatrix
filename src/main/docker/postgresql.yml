version: '2'
services:
  skillmatrix-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/skillmatrix/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=skillmatrix
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
